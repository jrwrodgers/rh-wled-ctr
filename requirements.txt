# rh-wled-ctr RotorHazard plugin dependencies
# Install: pip install -r requirements.txt

# HTTP client for WLED device API
requests>=2.28.0

# Web UI (Blueprint, jsonify, request). RotorHazard usually provides Flask.
flask>=2.2.0

# Optional: better network interface detection on Linux/macOS.
# Omit on Windows; plugin falls back to ipconfig/ifconfig.
netifaces>=0.11.0; sys_platform != "win32"
